/**
 * CONFIGURACIÓN DE ENTORNO - PRODUCCIÓN
 *
 * NOTA: apiUrl se actualizará con la URL real de Railway
 * una vez desplegado el backend.
 */

export const environment = {
    production: true,
    apiUrl: 'https://restocheck-backend-production.up.railway.app/api',  // ← Se actualiza tras desplegar backend
    appName: 'RestoCheck',
    version: '1.0.0'
};
